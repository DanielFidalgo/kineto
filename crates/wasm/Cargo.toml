[package]
name = "kineto-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
kineto-core = { path = "../core", default-features = false }
wasm-bindgen = "0.2"

[features]
corpus = []
