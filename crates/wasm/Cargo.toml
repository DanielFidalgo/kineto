[package]
name = "zoetrope-wasm"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zoetrope-core = { path = "../core", default-features = false }
wasm-bindgen = "0.2"

[features]
corpus = []
